# Docker image
DOCKER_IMAGE=your-dockerhub-username/lunch-app:latest

# Domain for HTTPS (must point to this server's IP)
DOMAIN=lunch.yourdomain.com

# Database credentials
DB_USERNAME=appuser
DB_PASSWORD=change-me-to-a-strong-password

# Application login credentials
APP_USERNAME=pavel
APP_PASSWORD=change-me-to-a-strong-password
